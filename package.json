{
  "name": "quoreit-crm",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd shared && npm install && cd ../backend && npm install && npm run build",
    "start": "node backend/dist/index.js"
  },
  "engines": {
    "node": ">=18"
  }
}
