[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = [
  "npm install --prefix ../shared",
  "npm install"
]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node dist/index.js"
